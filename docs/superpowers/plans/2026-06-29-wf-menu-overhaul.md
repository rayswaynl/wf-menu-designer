# WF Menu / HUD Designer — v2 Overhaul Plan (WYSIWYG, multi-display)

> REQUIRED SUB-SKILL: superpowers:subagent-driven-development. `- [ ]` steps. index.html edits are SEQUENTIAL. Built in two phases with a review gate after the renderer foundation.

**Goal:** rebuild the box-wireframe tool into a **true WYSIWYG, multi-display** Arma 2 dialog/HUD editor: controls render with their REAL styling on a 16:9 game frame; load/edit ANY of the ~26 WASP displays; pro editing (multi-select/undo/align/snap); full property inspector; faithful whole-display `.hpp` round-trip.

**Source (read-only)** — base = `C:\Users\Steff\a2waspwarfare\Missions\[55-2hc]warfarev2_073v48co.chernarus\Rsc`:
- `Dialogs.hpp` (~3602 lines) — ALL full-screen dialogs. `Titles.hpp` — ALL RscTitles HUD overlays. `Ressources.hpp` — the base control classes. `Styles.hpp` — the `WFBE_*` color/style macros. (`description.ext` `#include`s them.)

## Grounding (distilled — the old `dialogs.json` only had WF_Menu + WRONG IDDs)
**Displays (source-verified IDD · category · position-system):** Dialogs (unit_square_0_1, plain 0..1 floats): WF_Menu 11000 (hub, 10 ShortcutButtonMain tiles), WFBE_BuyGearMenu 503000 (~60 ctrls, most complex), RscMenu_BuyUnits 12000 (~37, map+factory tabs), RscMenu_Command 14000 (~31, commander console, map), RscMenu_Tactical 17000, RscMenu_Upgrade 18000 (icon-node tree), WFBE_UpgradeMenu 504000, RscMenu_Team 13000, RscMenu_Service 20000, WFBE_RespawnMenu 511000, WFBE_TransferMenu 505000, WFBE_VoteMenu 500000, WFBE_Commander_VoteMenu 500999, RscMenu_Economy 23000, RscMenu_EASA 23000 (⚠ idd collision w/ Economy), RscMenu_UnitCamera 21000, RscDisplay_Parameters 22000, RscMenu_Help 508000 (⚠ uses SafeZone exprs). HUD RscTitles (SafeZone exprs): OptionsAvailable 10200 (RUBHUD: money/supply/income/health label+value pairs idc 1346-1367 + 18 OptionsIcon0-17 idc 3500-3517), RscOverlay 10200 (⚠ collision), CaptureBar 600100, EndOfGameStats 90000 (raw 0..1), WFBE_ConstructionInterface 112200.
**Base style classes (`Ressources.hpp`) — defaults to RESOLVE + render:** RscText (type0, font Zeppelin32, sizeEx .03, shadow2, colorText {.933,.898,.545,.9} tan, colorBackground {0,0,0,0} transparent), RscText_Title (blue {.2588,.7137,1,1}, sizeEx .045), RscButton (type1, colorText {1,1,1,.8}, colorBackground {.588,.588,.353,.7} olive, colorFocused same, sizeEx .035), RscButton_Main (colorBackground {.2588,.7137,1,.7} WASP-blue), RscButton_Exit (colorBackground {.502,0,0,1} red, text "X"), RscShortcutButton/RscShortcutButtonMain (type16 tile, color {.2588,.7137,1,1} blue label, anim textures — render as a bordered tile w/ left-aligned label), RscListBox/RscListnBox (type5/102, colorBackground {0,0,0,1} black, colorText {1,1,1,.75}, blue select), RscCombo (type4, colorBackground {0,0,0,.75}, blue arrow), RscEdit (type2, colorText {1,1,1,.75}), RscStructuredText (type13), RscPicture (type0 style48, image), RscClickableText (type11, icon slot — colorText tints the icon), RscXSliderH (type43), IGUIBack (type0 style128, colorBackground {0,0,0,.6}), RscFrame (type0 style64, border-only, white line), RscControlsGroup (type15), RscMapControl (type101 — render as a grey placeholder w/ "MAP"). Fonts: **Zeppelin32** (all text → map to a clean sans like Inter/system), EtelkaNarrowMediumPro (map labels). shadow: 0 none /1 outline /2 drop.
**Color macros (`Styles.hpp` → RGBA):** WFBE_Background_Color {0,0,0,.7}, _Header {0,0,0,.4}, _Footer {0,0,0,.3}, WFBE_Background_Border {.2588,.7137,1,1} (WASP blue), WFBE_Menu_Button_Color {.2588,.7137,1,.7}, _Text {1,1,1,.8}, _Focused {.2588,.7137,1,1}, _Sub {.388,.925,.494,.7} mint, WFBE_Menu_Text_Color {.2588,.7137,1,1}. Resolve macros → RGBA for render; KEEP the macro name on export.
**Position systems:** dialogs = plain floats, fraction of screen → `px = v*screenW/H`. HUD = SafeZone string expressions (store as string, eval for preview with `safezoneX=0.07, safezoneY=0.07, safezoneW=0.86, safezoneH=0.86`). RGBA `{r,g,b,a}` are 0..1 → CSS `rgba(r*255,...,a)`.
**Export structure:** `class <Dlg> { idd; movingEnable; class controlsBackground{ class C:Base{ x;y;w;h; overrides } }; class controls{ class C:Base{ idc; x;y;w;h; text; action; overrides } } }`. RscTitles add a `controls[]={"C1",...}` name array. Inheritance: each control `: BaseClass`, export ONLY the delta (overridden props), keep `WFBE_*` macro names, preserve sub-class blocks (HitZone/ShortcutPos/TextPos/Attributes/ScrollBar) verbatim.

---
## PHASE 1 — data + WYSIWYG renderer (review gate after)

### Task 1: Generator — parse ALL displays + base styles + macros
**Files:** rewrite `tools/extract_dialogs.py` (+ tests).
- [ ] Parse `Styles.hpp` → `macros` (name → RGBA / value). Parse `Ressources.hpp` → `baseClasses` (name → {parent, props (type/style/colors/font/sizeEx/...), subClasses verbatim}). Parse `Dialogs.hpp` + `Titles.hpp` → every display (correct idd, category, posSystem, controlsBackground[], controls[], the RscTitles `controls[]` array) with each control's `{name, baseClass, idc, props (incl x/y/w/h as float OR string-expr), subClasses}`.
- [ ] For each control, compute `resolved` props by walking baseClass→parent chain (+ resolving macro refs to RGBA via `macros`), but KEEP the raw overrides separately (for delta export). Emit `assets/data/ui.json = { macros, baseClasses, displays:{ <name>:{ idd, category, posSystem:'unit_square'|'safezone', movingEnable, background:[...], controls:[...], titlesArray?:[...] } } }`.
- [ ] Tests on inline fixtures (a macro, a base class w/ inheritance, a dialog control w/ override, a SafeZone string x). Run → ui.json. Sanity: ~24+ displays; WF_Menu has 10 ShortcutButtonMain; baseClasses has RscButton/RscShortcutButton; macros has WFBE_Menu_Button_Color. **Commit** `feat(tools): parse all WASP displays + base styles + macros -> ui.json`.

### Task 2: WYSIWYG renderer + canvas + display picker (THE foundation)
**Files:** rewrite `index.html` (keep the WDDM theme/brandbar).
- [ ] **Display picker**: a dropdown of all displays grouped by category; selecting loads it.
- [ ] **16:9 game frame**: a stage rendering at a chosen screen res (default 1920×1080), zoom/pan (scroll+drag), a subtle dark backdrop. Map control coords → px (unit_square: `v*W/H`; safezone: eval the expression with the constants).
- [ ] **WYSIWYG control rendering**: render EACH control as a real-styled `<div>` using its `resolved` props — colorBackground→CSS bg rgba, colorText→text color, font→the mapped font, sizeEx→font-size (≈ sizeEx*screenH px), borders/shadow per style. Per type: RscText→styled text; RscButton/RscButton_Main/_Exit→a filled button w/ the olive/blue/red bg + centered/left label; RscShortcutButton(Main)→a bordered tile w/ left label (the blue hub look); RscListBox/nBox→a black panel w/ rows + a header bar; RscCombo→a field w/ a ▾; RscEdit→a bordered field; RscStructuredText→text; RscPicture/ClickableText→an icon placeholder tinted by colorText; RscFrame→border-only; IGUIBack→translucent panel; RscMapControl→a grey "MAP" placeholder; RscXSliderH→a slider bar. The result must LOOK like the real menu (dark panels, WASP-blue accents).
- [ ] **Edit/Preview toggle**: Preview = clean render; Edit = render + selection handles + a faint outline per control + the layer affordances.
- [ ] Basic select + drag/resize (carry over from v1) so it's usable; full pro-editing is Phase 2.
- [ ] Verify (Playwright): load WF_Menu → the 10 tiles render as blue bordered tiles w/ labels on a centered dark panel (looks like the menu, not boxes); switch to OptionsAvailable (RUBHUD) → the money/supply label+value pairs render at their SafeZone positions; switch to RscMenu_BuyUnits → renders. 0 console errors. Screenshot WF_Menu + RUBHUD (this is the review gate). **Commit** `feat: WYSIWYG renderer + display picker + 16:9 canvas`.

---
## PHASE 2 — render-fidelity fixes + pro editing + inspector + faithful export (on the verified foundation; Phase 1 = `904f0b3` gen + `0b3d650` renderer)

### Task 2.5: Render fidelity fixes (FIRST — gate review found these)
Phase-1 review: dialogs (unit_square) render great; two defects:
- [ ] **SafeZone HUD eval is broken.** On OptionsAvailable/RUBHUD the label+value pairs collapse into ONE overlapping blob and the OptionsIcon strip renders on the LEFT (should hug the right edge). Diagnose: read the actual x/y/w/h string expressions in `ui.json` for OptionsAvailable's controls + the renderer's evaluator. The per-row y exprs (e.g. `0.2075 * safezoneH + safezoneY`, different multiplier per row) + the icon x (`(SafeZoneW + SafeZoneX) - 0.0392157` etc.) must each evaluate to DISTINCT pixel positions. Fix the evaluator (handle `safezoneX/Y/W/H` case-insensitively, `min`/`max`, multiply/add/subtract/paren) so each control lands at its real spot — RUBHUD pairs spread down the right side, icon strip at the right edge. Verify on OptionsAvailable + CaptureBar + WFBE_ConstructionInterface: controls are SEPARATED + correctly placed.
- [ ] **Preview clutter.** In Preview mode, controls with empty `text` (decorative panels: Background_M/H/F/L, IGUIBack, frames) must render NO text — just their panel/bg/border. The control NAME label only shows in EDIT mode (as an overlay tag), never as the control's own text. (Currently "Background_M"/"Background_F" etc. print as body text over the menu.)
- [ ] Re-verify (Playwright): WF_Menu preview is clean (no stray panel-name text); OptionsAvailable HUD reads correctly (separated rows, right-edge icons). 0 errors. Screenshot both. Commit `fix: SafeZone HUD coord eval + clean preview (no decorative name text)`.

### Task 3: Pro editing
- [ ] Marquee + shift/ctrl multi-select; align (L/R/T/B/center) + distribute; copy/paste/duplicate; **undo/redo** (full history); arrow-key nudge (+shift = ×10); smart snapping (grid + snap-to-control edges/centers w/ guide lines); z-order (raise/lower/front/back); group/ungroup; per-layer lock + hide; click-through overlap select (alt-click cycles overlapping). Add control from a palette (typed, w/ a fresh idc, into controls or controlsBackground). Commit `feat: pro editing — multi-select, undo, align, snap, z-order`.

### Task 4: Full property inspector
- [ ] Inspector edits ALL props for the selected control(s): idc, name, baseClass (dropdown of baseClasses), x/y/w/h (number OR string-expr toggle for HUD), text, the color props (colorBackground/colorText/colorFocused/... via color pickers that read/write RGBA, w/ a macro dropdown to assign a `WFBE_*` macro), font, sizeEx, style (flag checkboxes), shadow, tooltip, action/onButtonClick/onLBSelChanged. Show inherited (from baseClass) vs overridden (highlight overrides; "reset to inherited" per prop). Multi-select edits common props. Commit `feat: full property inspector (colors/font/style/events + inheritance)`.

### Task 5: Faithful import + export + round-trip
- [ ] **Export** the WHOLE selected display as valid `.hpp`: `class <Dlg> { idd; movingEnable; class controlsBackground{...}; class controls{...} }` (+ the RscTitles `controls[]` array for HUD), each control `: BaseClass` emitting ONLY overridden props (the delta), keeping `WFBE_*` macro NAMES, preserving sub-class blocks verbatim. **Import**: paste any display's `.hpp` (or a `class controls{}` block) → parse (JS port) → load it editable. Copy + download.
- [ ] **Round-trip gate (Playwright)**: load WF_Menu from ui.json → export → the output re-parses to the same controls (idc/baseClass/geometry/overrides); paste the real WF_Menu .hpp → export → re-parses equal. Move one control → only its x/y change in the delta. 0 errors. Commit `feat: faithful whole-display .hpp export + import + round-trip`.

### Task 6: finish + deploy
- [ ] tests pass; full smoke (multi-display render, editing, inspector, round-trip); 0 errors; screenshots. README v2. Controller: merge feat/v2-overhaul → main → Pages; verify live (the hub tile auto-updates).

## Self-Review
- WYSIWYG real-styling (Task 2) = the centerpiece + review gate; multi-display (24+); pro editing (Task 3); full props + inheritance (Task 4); faithful whole-display round-trip keeping macros/sub-classes (Task 5). Sequential index.html edits.
